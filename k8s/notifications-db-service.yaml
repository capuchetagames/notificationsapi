apiVersion: v1
kind: Service
metadata:
  name: notifications-db
  namespace: fiapstore
  labels:
    app: notifications-db
spec:
  type: ClusterIP
  selector:
    app: notifications-db
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
