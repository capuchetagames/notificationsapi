apiVersion: v1
kind: Service
metadata:
  name: notifications-api
  namespace: fiapstore
  labels:
    app: notifications-api
spec:
  type: LoadBalancer
  selector:
    app: notifications-api
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
